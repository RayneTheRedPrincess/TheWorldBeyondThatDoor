import {escapeHtml,shell} from './shared.js';
export function renderHelp({entries=[],query=''}){const q=String(query||'').trim().toLowerCase();const shown=entries.filter(e=>!q||`${e.title} ${e.body}`.toLowerCase().includes(q));return shell(`<section class="section-title"><div><div class="kicker">Tutorials & Help</div><h2>Help Codex</h2><div class="muted">Exact current mechanics, searchable by term.</div></div><div class="button-row"><button class="secondary" data-action="back-to-tavern">Return to Main Hall</button><button class="secondary" data-action="home">Return Home</button></div></section><section class="panel section"><label class="help-search"><span>Search the Codex</span><input type="search" value="${escapeHtml(query)}" data-help-search placeholder="Try Energy, Block, Trainers, lender…"/></label><div class="help-grid section">${shown.length?shown.map(e=>`<details class="help-entry" ${q?'open':''}><summary>${escapeHtml(e.title)}</summary><p>${escapeHtml(e.body)}</p></details>`).join(''):'<div class="empty-state">No Help entries match that search.</div>'}</div></section>`,{back:true});}
